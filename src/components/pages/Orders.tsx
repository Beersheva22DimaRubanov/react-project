import { Typography } from "@mui/material"

export const Orders: React.FC = () => {

    return  <Typography variant="h3" sx ={{textAlign: 'center', fontSize: {xs: '3em', sm: '2em'}}} component={"div"}>Orders component</Typography>
}